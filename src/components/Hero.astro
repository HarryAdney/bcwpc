---
// import { Icon } from 'astro-icon/components'

const { src = '/walden-west-burton-1080x499.webp' } = Astro.props
---

<section class="hero my-24">
  <div class="container">
    <div class="grid grid-cols-1 items-center lg:grid-cols-1">
      <!-- <div class="flex flex-col items-center md:items-start">
        <h1 class="text-4xl text-center md:text-left">
          <slot>Welcome to West Burton, North Yorkshire</slot>
        </h1>
      </div> -->
      <img
        class="hidden lg:block"
        src={src}
        alt="View of West Burton, nestled among the surrounding hills"
        width="1049"
        height="499"
        decoding="async"
      />
    </div>
  </div>
</section>

<style lang="scss">
  :global(.darkmode .text-gradient) {
    background: linear-gradient(315deg, var(--primary-200) 25%, var(--secondary-400));
    background-clip: border-box;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
</style>
